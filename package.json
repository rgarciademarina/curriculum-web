{
  "name": "curriculum-web",
  "version": "1.0.0",
  "description": "Curriculum web personal",
  "scripts": {
    "start": "http-server -p 8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}